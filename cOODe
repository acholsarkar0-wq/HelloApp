public class HelloUser {

    public static void main(String[] args) {

        String name = "World"; // default value

        // Check if a name is provided in command line
        if (args.length > 0) {
            name = args[0];
        }

        // Display greeting
        System.out.println("Hello " + name);
    }
}
